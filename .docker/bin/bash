#!/bin/bash

if [ ${0} != ".docker/bin/bash" ]; then
  echo "You must run the script from the root folder"
  exit 0
fi

export WWWUSER=${WWWUSER:-$UID}
export WWWGROUP=${WWWGROUP:-$(id -g)}

docker compose -f api-laravel/docker-compose.yml exec -it -u sail  laravel.test bash